{
  "name": "bencodex",
  "version": "0.1.1",
  "description": "bencodex",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "engines": {
    "node": ">=11"
  },
  "scripts": {
    "build": "rimraf ./lib && tsc",
    "prepublishOnly": "npm run build",
    "test": "cd test && npm install && npm test"
  },
  "keywords": [
    "bencode",
    "bencodex"
  ],
  "author": "JongChan Choi <jong@chan.moe>",
  "license": "Zlib",
  "devDependencies": {
    "@types/node": "^10.12.9",
    "rimraf": "^2.6.2",
    "typescript": "^3.2.0-rc"
  },
  "dependencies": {
    "is-buffer": "^2.0.5"
  }
}
